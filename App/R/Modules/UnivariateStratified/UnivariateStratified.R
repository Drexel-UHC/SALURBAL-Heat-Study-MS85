UnivariateStratified_UI <- function(id) {
  ns <- NS(id)
  tagList(
    "Univariate Stratified"
  )
}

UnivariateStratified_Server <- function(id){
  moduleServer(id,function(input, output, session) {
  
  })
}