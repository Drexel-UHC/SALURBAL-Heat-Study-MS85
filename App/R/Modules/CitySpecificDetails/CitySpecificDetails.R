CitySpecificDetails_UI <- function(id) {
  ns <- NS(id)
  tagList(
    "City Specific Details"
  )
}

CitySpecificDetails_Server <- function(id,data, option){
  moduleServer(id,function(input, output, session) {
  
  })
}